.DS_Store

# app (Vite/Node)
app/node_modules/
app/dist/
app/.vite/

# src-tauri (Rust)
src-tauri/target/
src-tauri/gen/

# Local-only tooling: records docs/demo.gif from the running app.
# Kept out of the repo because it needs playwright-core (13 MB) that CI
# would otherwise install on every run.
scripts/record-demo.mjs
