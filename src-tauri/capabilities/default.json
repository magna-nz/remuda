{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Remuda window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
